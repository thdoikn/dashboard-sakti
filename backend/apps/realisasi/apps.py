from django.apps import AppConfig


class RealisasiConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.realisasi"
    verbose_name = "Realisasi (Disbursement)"
